<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title>Jenkins 대기 시간을 약 25초 수준으로 줄이기 위해 Claude Code 스킬을 만든 기록</title>
      <link href="/2026/05/26/aws-deploy-skill-claude-code-final-bygpt/"/>
      <url>/2026/05/26/aws-deploy-skill-claude-code-final-bygpt/</url>
      
        <content type="html"><![CDATA[<blockquote><p><strong>대상 독자</strong>: AWS Lambda와 Step Functions를 직접 배포하는 개발자, 그리고 Claude Code를 실무에 붙여보려는 DevOps&#x2F;인프라 담당자.<br><strong>전제 조건</strong>: AWS CLI 설정 및 Claude Code CLI 설치 완료.</p></blockquote><p>문제는 단순한 빌드 duration이 아니었다. Lambda 1개만 수정해도, 개발자가 체감하는 Jenkins 대기 시간은 생각보다 길었다.</p><p>Lambda 1개 수정 케이스에서 Jenkins는 Hook 수신부터 배포 완료까지 실측 67초였다. 평균 체감 시간으로 보면 약 2분 44초 수준이었다.</p><p>aws-deploy 스킬로 바꾼 뒤 같은 케이스에서 변경 감지 → 분석 보고 완료까지는 4.57초가 나왔다. 실제 배포를 끝내기 전, Stage 1 분석부터 Lambda 코드 zip·업로드까지 포함한 체감 총시간은 약 25초 <code>[추정]</code>로 보는 편이 더 보수적이다.</p><p>숫자를 바로 한 줄로 묶어 비교하면 오해가 생긴다. <code>4.57초</code>는 <strong>Stage 1 분석 시간 실측값</strong>이고, 실사용 체감으로 비교하면 <code>67초 vs 약 25초 [추정]</code>에 더 가깝다.</p><p>이 숫자 차이가 이 글의 포인트는 아니다. 더 중요한 건 Jenkins 파이프라인에 없던 <strong>Step Functions definition 교체와 SF version·alias 핀닝</strong>이 이 스킬에는 들어 있다는 점이다. 속도를 줄인 것보다, <strong>배포가 완결되는 기준 자체를 다시 정의했다</strong>는 쪽이 더 정확하다.</p><span id="more"></span><hr><h2 id="Claude-Code-스킬이란"><a href="#Claude-Code-스킬이란" class="headerlink" title="Claude Code 스킬이란"></a>Claude Code 스킬이란</h2><p>Claude Code의 Skills는 Claude Code 안에서 동작하는 마크다운 기반 프로시저다. MCP처럼 외부 서버를 띄우지 않아도 되고, <code>.claude/commands/</code>나 프로젝트 내부 파일로 관리할 수 있다. “배포해줘” 같은 자연어 입력이 트리거가 되고, 그 뒤에 연결된 절차를 명령과 규칙으로 실행한다. 이번 글의 aws-deploy는 CI 대체 플랫폼이라기보다, <strong>CLI에서 반복하던 배포 루틴을 재사용 가능한 프로시저로 만든 것</strong>에 가깝다.</p><hr><h2 id="Jenkins가-느린-두-가지-이유"><a href="#Jenkins가-느린-두-가지-이유" class="headerlink" title="Jenkins가 느린 두 가지 이유"></a>Jenkins가 느린 두 가지 이유</h2><p>Jenkins 빌드 히스토리를 보면 최소 41초, 최대 6분 18초다.</p><p><img src="/images/aws-deploy-skill/jenkins-build-history.png" alt="Jenkins 빌드 히스토리 (n&#x3D;14)"></p><p>n&#x3D;14 기준 평균 빌드 시간은 139.5초, 대기 포함 총 평균은 약 2분 27초다. 그런데 개발자가 체감하는 시간은 이것보다 길다.</p><p>Lambda 1개 수정 케이스 실측값이다.</p><table><thead><tr><th>구간</th><th>시각 (UTC)</th><th>소요</th></tr></thead><tbody><tr><td>Hook 수신</td><td>07:03:56</td><td>—</td></tr><tr><td>작업 시작 (scheduled)</td><td>07:04:06</td><td>+10초</td></tr><tr><td>대기 (queue wait)</td><td>—</td><td>7.2초</td></tr><tr><td>빌드 duration</td><td>—</td><td>49초</td></tr><tr><td><strong>Hook 수신 → 완료</strong></td><td></td><td><strong>67초</strong></td></tr></tbody></table><p>빌드 duration만 보면 49초지만, 개발자가 실제로 기다리는 시간은 67초다. 이 오버헤드가 평균 빌드에도 비슷하게 붙는다고 보면, n&#x3D;14 평균 체감 총 소요는 약 <strong>2분 44초</strong>로 보는 편이 현실에 가깝다.</p><p>편차의 첫 번째 이유는 변경 감지 기준이다. 이 Jenkins 파이프라인도 변경된 Lambda만 추려서 배포한다. 다만 기준이 <code>HEAD~1..HEAD</code> 단일 커밋 diff라서 여러 커밋을 한 번에 push하면 첫 커밋 변경이 빠질 수 있고, 미커밋 변경은 아예 감지하지 못한다. 즉 차이는 “변경 Lambda 필터링 유무”가 아니라, <strong>변경 감지 기준의 정밀도와 포함 범위</strong>에 있다.</p><p>두 번째 이유는 Hook 콜드 스타트와 파이프라인 공통 오버헤드다. <code>git push</code> 이후 Webhook 수신 → Jenkins 스케줄 → 큐 대기 → 실제 빌드 시작까지의 시간은 배포 대상이 1개든 10개든 앞에 붙는다.</p><p>속도만의 문제도 아니다. Lambda alias는 <code>&#123;ENV&#125;-&#123;빌드번호&#125;</code> 형식으로 자동 생성되고, 변경된 Lambda만 골라 배포하는 로직도 이미 있다. 하지만 Step Functions definition 교체·SF version publish·SF alias 핀닝은 파이프라인에 없었다. 즉 이 Jenkins는 <strong>변경 Lambda 선택 배포 파이프라인</strong>이긴 했지만, 서버리스 전체 배포 체인을 끝까지 묶는 자동화는 아니었다. 정확히 말하면 <strong>Lambda 코드 업로드 자동화</strong>에 더 가까운 상태였다.</p><hr><h2 id="전환점-“DEV에-배포해줘”가-재현한-수동-루틴"><a href="#전환점-“DEV에-배포해줘”가-재현한-수동-루틴" class="headerlink" title="전환점: “DEV에 배포해줘”가 재현한 수동 루틴"></a>전환점: “DEV에 배포해줘”가 재현한 수동 루틴</h2><p>이 전환은 성능 실험에서 시작된 게 아니다. 오히려 몇 번의 우회에서 시작됐다.</p><p>AI 개발 속도가 빨라지면서 “코드 수정 → merge → Jenkins 대기 2분 44초”의 호흡이 답답해졌다. 급한 상황에서는 Jenkins를 기다리지 않고 AWS CLI로 직접 배포한 적도 몇 번 있었다. 수동이라 번거롭지만, 적어도 어떤 Lambda를 어떤 순서로 올리고 어떤 alias를 바꾸는지는 내가 즉시 통제할 수 있었다.</p><p>그러다 어느 날 Claude Code CLI에 “DEV에 배포해줘”라고 입력했더니, 내가 이전에 수동으로 밟았던 절차와 유사한 순서를 재현했다. 여기서 중요한 건 “Claude가 마법처럼 배포를 이해했다”가 아니다. <strong>반복하던 절차가 이미 언어화 가능한 패턴이었고, 세션 문맥 안에서 그 순서를 재현할 수 있다는 게 보였다</strong>는 점이다. 그걸 보고 “이건 Skill로 굳힐 수 있겠다”는 판단이 생겼다.</p><p>그 순간 판단이 섰다.</p><blockquote><p>파이프라인에서 하던 일 중 일부는 스크립트로, 나머지는 Skill로 만들 수 있겠다.</p></blockquote><p>이 지점이 없으면 이 글은 빠른 배포 도구 비교로만 읽힌다. 실제로는 <strong>수동 루틴이 재사용 가능한 배포 프로시저로 응고된 과정</strong>이 핵심이다.</p><hr><h2 id="왜-GitHub-Actions도-아니고-MCP도-아니고-Skills였나"><a href="#왜-GitHub-Actions도-아니고-MCP도-아니고-Skills였나" class="headerlink" title="왜 GitHub Actions도 아니고, MCP도 아니고, Skills였나"></a>왜 GitHub Actions도 아니고, MCP도 아니고, Skills였나</h2><p>선택지는 있었다.</p><ul><li><strong>GitHub Actions</strong>: 실행 주체가 CI 서버다. 재현성은 좋지만, 로컬 CLI 맥락과 분리된다.</li><li><strong>별도 스크립트</strong>: 동작은 만들 수 있지만, 어떤 조건에서 무엇을 판단하는지가 코드 밖으로 흩어지기 쉽다.</li><li><strong>MCP 서버</strong>: 외부 도구 연동에는 좋다. 하지만 이번 문제는 외부 서버 확장보다, 로컬 작업 루틴의 코드화가 목적이었다.</li></ul><p>이번 케이스에서 중요한 건 실행 위치였다. 배포 권한은 로컬 AWS CLI 프로파일과 작업자 맥락에 묶여 있었고, Step Functions definition 교체와 alias 정합성 확인도 로컬에서 바로 확인하는 흐름이 더 빨랐다. 즉, 필요했던 건 “또 하나의 중앙 실행 주체”가 아니라, <strong>내가 실제로 일하는 자리에서 배포 판단과 실행을 이어붙이는 방식</strong>이었다. 그 용도에는 Skills가 가장 잘 맞았다.</p><hr><h2 id="스킬의-핵심-바뀐-것만-본다"><a href="#스킬의-핵심-바뀐-것만-본다" class="headerlink" title="스킬의 핵심: 바뀐 것만 본다"></a>스킬의 핵심: 바뀐 것만 본다</h2><p>aws-deploy 스킬이 가장 먼저 하는 일은 변경 파일 목록을 만드는 것이다.</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># BASE 결정 — 우선순위 순</span></span><br><span class="line">BRANCH=$(git branch --show-current)</span><br><span class="line">git fetch origin <span class="string">&quot;<span class="variable">$BRANCH</span>&quot;</span> --quiet 2&gt;/dev/null || <span class="literal">true</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> git rev-parse --abbrev-ref --symbolic-full-name <span class="string">&#x27;@&#123;u&#125;&#x27;</span> &gt;/dev/null 2&gt;&amp;1; <span class="keyword">then</span></span><br><span class="line">  BASE=$(git rev-parse <span class="string">&#x27;@&#123;u&#125;&#x27;</span>)</span><br><span class="line"><span class="keyword">elif</span> git rev-parse --verify <span class="string">&quot;origin/<span class="variable">$&#123;BRANCH&#125;</span>&quot;</span> &gt;/dev/null 2&gt;&amp;1; <span class="keyword">then</span></span><br><span class="line">  BASE=$(git rev-parse <span class="string">&quot;origin/<span class="variable">$&#123;BRANCH&#125;</span>&quot;</span>)</span><br><span class="line"><span class="keyword">else</span></span><br><span class="line">  BASE=$(git rev-parse HEAD~1)</span><br><span class="line"><span class="keyword">fi</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 커밋 변경 + 미커밋 변경 합산</span></span><br><span class="line">COMMITTED_FILES=$(git diff --name-only <span class="string">&quot;<span class="variable">$&#123;BASE&#125;</span>..HEAD&quot;</span>)</span><br><span class="line">UNCOMMITTED_FILES=$(git status --porcelain | awk <span class="string">&#x27;&#123;print $NF&#125;&#x27;</span>)</span><br><span class="line">CHANGED_FILES=$(<span class="built_in">printf</span> <span class="string">&#x27;%s\n%s\n&#x27;</span> <span class="string">&quot;<span class="variable">$COMMITTED_FILES</span>&quot;</span> <span class="string">&quot;<span class="variable">$UNCOMMITTED_FILES</span>&quot;</span> | <span class="built_in">sort</span> -u | grep -v <span class="string">&#x27;^$&#x27;</span>)</span><br></pre></td></tr></table></figure><p><code>HEAD~1..HEAD</code> 비교는 안 쓴다. 여러 커밋을 한 번에 push하면 첫 커밋 변경이 빠질 수 있기 때문이다. upstream(<code>@&#123;u&#125;</code>) → <code>origin/&#123;branch&#125;</code> → <code>HEAD~1</code> 순으로 fallback하고, 미커밋 변경도 합산한다.</p><p>이 목록에서 <code>lambda-functions/&#123;NAME&#125;/</code> 패턴으로 Lambda 디렉토리를 추출한다. 이름에서 <code>PROFILE</code> → 환경 prefix로 치환하면 배포 대상 Lambda 이름이 나온다.</p><p>여기서 중요한 점이 하나 있다. Jenkins도 이미 비슷한 아이디어를 갖고 있다. 실제 Jenkins 파이프라인 역시 <code>git diff --name-only HEAD~1 HEAD</code> 기준으로 changed files를 뽑고, 그중 <code>lambda-functions/</code> 경로에 해당하는 Lambda만 감지해서 배포한다. 따라서 aws-deploy의 차별점은 “Jenkins는 전체를 다 배포하고 Skill은 변경분만 배포한다”가 아니다. <strong>Jenkins는 단일 커밋 기준 변경 Lambda 필터링, aws-deploy는 upstream 기준 + 미커밋 포함 + 후속 영향 분석까지 이어지는 필터링</strong>에 가깝다.</p><p>실제 실행 결과:</p><p><img src="/images/aws-deploy-skill/change-detection-result.png" alt="Stage 1 변경 감지 — {서비스명}-PROFILE-EDU-functionName → {서비스명}-DEV-EDU-functionName 감지"></p><p><code>lambda_function.py</code> 파일 1개 수정 → <code>&#123;서비스명&#125;-DEV-EDU-functionName</code> 1개만 배포 대상으로 잡혔다.</p><hr><h2 id="4단계-배포-체인-—-Lambda-코드-업로드가-끝이-아닌-이유"><a href="#4단계-배포-체인-—-Lambda-코드-업로드가-끝이-아닌-이유" class="headerlink" title="4단계 배포 체인 — Lambda 코드 업로드가 끝이 아닌 이유"></a>4단계 배포 체인 — Lambda 코드 업로드가 끝이 아닌 이유</h2><p>Lambda에 코드를 올리는 건 시작일 뿐이다. 실 트래픽이 새 코드를 타려면 publish → alias 핀닝 → Step Functions 반영이 이어져야 한다.</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">┌─────────────┬─────────────┬─────────────┬─────────────────┐</span><br><span class="line">│   Stage 2   │   Stage 3   │   Stage 4   │    Stage 5      │</span><br><span class="line">│  Lambda     │  Lambda     │  Lambda     │  Step Functions │</span><br><span class="line">│  코드 배포  │  publish    │  alias 핀닝 │  version+alias  │</span><br><span class="line">│             │             │             │  핀닝           │</span><br><span class="line">│  update-    │  publish-   │  update-    │  update-state-  │</span><br><span class="line">│  function-  │  version    │  alias      │  machine +      │</span><br><span class="line">│  code       │  → v30      │  DEV-       │  publish +      │</span><br><span class="line">│             │             │  20260526-1 │  alias 교체     │</span><br><span class="line">└─────────────┴─────────────┴─────────────┴─────────────────┘</span><br><span class="line">※ Lambda publish ≠ SF alias 자동 핀닝 — 둘 다 별도 수행 필수</span><br></pre></td></tr></table></figure><p>이 체인을 “원자적”이라고 부르면 안 된다. Stage 3 publish가 실패하면 Stage 2의 코드 업로드는 이미 반영된 상태로 남는다. 즉, 이 체인은 <strong>정합성을 맞추기 위해 순서대로 수행되는 배포 절차</strong>이지, 전체 성공&#x2F;전체 롤백이 보장되는 원자 연산은 아니다.</p><p>Stage 3에서 alias 이름은 배포 시작 시 딱 1번 결정한다. 당일 날짜 기준 기존 alias SEQ 최댓값 + 1.</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line">TODAY=$(<span class="built_in">date</span> +%Y%m%d)</span><br><span class="line">MAX_SEQ=0</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> fn <span class="keyword">in</span> <span class="variable">$TARGET_LAMBDAS</span>; <span class="keyword">do</span></span><br><span class="line">  names=$(aws lambda list-aliases --function-name <span class="string">&quot;<span class="variable">$fn</span>&quot;</span> \</span><br><span class="line">    --query <span class="string">&quot;Aliases[?starts_with(Name, &#x27;<span class="variable">$&#123;ENV_PREFIX&#125;</span>-<span class="variable">$&#123;TODAY&#125;</span>-&#x27;)].Name&quot;</span> \</span><br><span class="line">    --output text 2&gt;/dev/null || <span class="literal">true</span>)</span><br><span class="line">  <span class="keyword">for</span> n <span class="keyword">in</span> <span class="variable">$names</span>; <span class="keyword">do</span></span><br><span class="line">    <span class="built_in">seq</span>=<span class="string">&quot;<span class="variable">$&#123;n##*-&#125;</span>&quot;</span></span><br><span class="line">    [[ <span class="string">&quot;<span class="variable">$seq</span>&quot;</span> =~ ^[0-9]+$ ]] &amp;&amp; (( seq &gt; MAX_SEQ )) &amp;&amp; MAX_SEQ=<span class="variable">$seq</span></span><br><span class="line">  <span class="keyword">done</span></span><br><span class="line"><span class="keyword">done</span></span><br><span class="line"></span><br><span class="line">SEQ=$((MAX_SEQ + <span class="number">1</span>))</span><br><span class="line">ALIAS=<span class="string">&quot;<span class="variable">$&#123;ENV_PREFIX&#125;</span>-<span class="variable">$&#123;TODAY&#125;</span>-<span class="variable">$&#123;SEQ&#125;</span>&quot;</span>  <span class="comment"># 예: DEV-20260526-1</span></span><br></pre></td></tr></table></figure><p>Stage 5가 이 체인에서 가장 중요하다. Step Functions definition 안에 Lambda ARN이 버전 없이 박혀 있거나 이전 alias를 참조하면, Lambda 코드를 올려도 Step Functions는 이전 버전을 그대로 호출한다.</p><p>같은 배포 안의 Lambda와 Step Functions가 동일한 alias 이름을 쓰게 맞추는 이유는 단순하다. Stage 5에서 definition 안의 Lambda ARN을 교체할 때, <strong>어느 버전 세트를 묶어 배포했는지 추적 가능해야 하기 때문</strong>이다.</p><hr><h2 id="“배포해줘”-한-마디가-실제로-하는-일"><a href="#“배포해줘”-한-마디가-실제로-하는-일" class="headerlink" title="“배포해줘” 한 마디가 실제로 하는 일"></a>“배포해줘” 한 마디가 실제로 하는 일</h2><p>Claude Code 터미널에서 “배포해줘”를 입력하면 스킬이 Stage 1~5를 순차 실행한다. 배포 전에 먼저 분석 결과를 보여준다.</p><p><img src="/images/aws-deploy-skill/dry-run-result.png" alt="aws-deploy 스킬 dry-run 분석 결과 — Lambda 1개, SF 2개 영향 감지"></p><p>Lambda 1개 수정했는데 SF가 2개 뜬 것은, 두 Step Functions definition 소스 파일 안에 해당 Lambda ARN이 있어서다. 스킬은 <strong>로컬 저장소 안의 Step Functions 정의 파일을 grep해서</strong> 연관 대상을 잡아낸다. 즉 “현재 AWS에 배포된 state machine 정의”를 직접 조회하는 방식이 아니라, <strong>배포 대상이 되는 로컬 정의 파일 기준의 영향 분석</strong>이다.</p><p>이 점은 한계이기도 하다. 로컬 정의와 AWS에 이미 배포된 정의가 어긋나 있다면 영향 분석 결과도 그 차이를 그대로 안고 간다. 대신 이 글의 전제는 “정의 파일이 저장소의 기준 진실(source of truth)이다”라는 쪽에 가깝다.</p><p>이후 실제 배포가 끝나면 <strong>Stage 6 dry-run 검증</strong>까지 자동으로 이어질 수 있다. Jenkins 비교에서 이 항목이 중요한 이유는, 기존 파이프라인에는 같은 검증 단계가 별도로 없었기 때문이다.</p><hr><h2 id="프로젝트-확장-projects-서비스명-md-하나면-된다"><a href="#프로젝트-확장-projects-서비스명-md-하나면-된다" class="headerlink" title="프로젝트 확장: projects/{서비스명}.md 하나면 된다"></a>프로젝트 확장: <code>projects/&#123;서비스명&#125;.md</code> 하나면 된다</h2><p>스킬 자체는 기존 프로젝트 기준으로 만들었다. 다른 프로젝트에 붙이려면 <code>projects/&#123;서비스명&#125;.md</code> 파일 하나만 추가하면 된다.</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># projects/&#123;서비스명&#125;.md (핵심 부분)</span></span><br><span class="line"></span><br><span class="line"><span class="attr">AWS Profile  :</span> &#123;<span class="string">aws-profile</span>&#125;</span><br><span class="line"><span class="attr">Account ID   :</span> &#123;<span class="string">AWS_ACCOUNT_ID</span>&#125;</span><br><span class="line"></span><br><span class="line"><span class="string">브랜치</span> <span class="string">→</span> <span class="string">환경:</span></span><br><span class="line">  <span class="string">develop</span> <span class="string">→</span> <span class="string">DEV</span></span><br><span class="line">  <span class="string">stg</span>     <span class="string">→</span> <span class="string">STG</span></span><br><span class="line">  <span class="string">real</span>    <span class="string">→</span> <span class="string">REAL</span></span><br><span class="line"></span><br><span class="line"><span class="attr">Lambda alias :</span> <span class="string">DEV-&#123;YYYYMMDD&#125;-&#123;SEQ&#125;</span></span><br><span class="line"><span class="attr">SF alias     :</span> <span class="number">0.0</span><span class="string">.x</span>  <span class="comment"># 백엔드가 alias 이름으로 직접 호출</span></span><br><span class="line"></span><br><span class="line"><span class="string">배포</span> <span class="string">제외:</span></span><br><span class="line">  <span class="bullet">-</span> &#123;<span class="string">서비스명</span>&#125;<span class="string">-PROFILE-*-router</span></span><br></pre></td></tr></table></figure><p>프로젝트마다 다른 부분 — AWS profile, alias 형식, 배포 제외 Lambda 목록 — 만 여기 적고, 스킬 코어는 건드리지 않는다.</p><p>이 구조의 장점은 단순한 재사용성보다 <strong>배포 규칙의 외부화</strong>다. 원래 내 머릿속에 있던 예외와 명명 규칙이 파일로 빠져나오는 순간, 개인 루틴이 팀이 읽을 수 있는 문서로 바뀐다.</p><hr><h2 id="숫자는-이렇게-읽는-편이-정직하다"><a href="#숫자는-이렇게-읽는-편이-정직하다" class="headerlink" title="숫자는 이렇게 읽는 편이 정직하다"></a>숫자는 이렇게 읽는 편이 정직하다</h2><p>비교에서 가장 위험한 실수는 서로 다른 층위의 시간을 한 줄에 섞는 것이다.</p><p>이 글의 숫자는 네 종류다.</p><ul><li><strong>67초</strong>: Jenkins의 Hook 수신 → 완료까지 최선 케이스</li><li><strong>약 2분 44초</strong>: Jenkins 평균 체감 총 소요</li><li><strong>4.57초</strong>: aws-deploy의 Stage 1 분석 완료 시간</li><li><strong>약 25초 <code>[추정]</code></strong>: aws-deploy의 Stage 1 분석부터 Lambda 코드 zip·업로드까지 포함한 체감 총시간</li></ul><p>따라서 <code>4.57초 vs 2분 44초</code>는 <strong>“무엇이 배포될지 파악하는 시간”</strong> 비교다. 반대로 실사용 체감으로 비교하면 <code>67초 vs 약 25초 [추정]</code>에 더 가깝다.</p><p>실사용 체감의 <code>약 25초</code>는 Lambda 1개 기준으로 <strong>Stage 1 분석부터 Lambda 코드 zip·업로드까지</strong>를 포함한 추정치다. Step Functions 후속 단계까지 실측한 숫자는 아니므로, 이 구간 역시 어디까지나 추정으로만 둔다.</p><h3 id="1-변경-분석-시간-비교"><a href="#1-변경-분석-시간-비교" class="headerlink" title="1. 변경 분석 시간 비교"></a>1. 변경 분석 시간 비교</h3><table><thead><tr><th>항목</th><th>Jenkins</th><th>aws-deploy 스킬</th></tr></thead><tbody><tr><td>“무엇이 배포되나” 파악 시간</td><td>평균 빌드 완료까지 대기</td><td><strong>4.57초</strong> (Stage 1 실측)</td></tr><tr><td>변경 감지 기준</td><td><code>HEAD~1..HEAD</code> 단일 커밋</td><td>upstream 기준 + 미커밋 포함</td></tr><tr><td>변경 Lambda 필터링</td><td>있음 (<code>lambda-functions/</code> 경로 기준)</td><td>있음 (<code>lambda-functions/</code> 경로 기준)</td></tr><tr><td>후속 영향 분석</td><td>없음 (Lambda 변경까지만)</td><td>있음 (연관 Step Functions grep)</td></tr></tbody></table><h3 id="2-배포-체인-비교"><a href="#2-배포-체인-비교" class="headerlink" title="2. 배포 체인 비교"></a>2. 배포 체인 비교</h3><table><thead><tr><th>항목</th><th>Jenkins</th><th>aws-deploy 스킬</th></tr></thead><tbody><tr><td>Lambda 1개 수정 (Hook 수신 → 완료)</td><td><strong>67초</strong> (실측)</td><td><strong>약 25초</strong> <code>[추정]</code> (Stage 1 분석부터 Lambda 코드 zip·업로드까지)</td></tr><tr><td>전체 평균 (n&#x3D;14, 콜드 스타트 포함)</td><td><strong>약 2분 44초</strong></td><td>—</td></tr><tr><td>Lambda 코드 업데이트</td><td>있음</td><td>있음</td></tr><tr><td>Lambda version publish</td><td>있음</td><td>있음</td></tr><tr><td>Lambda alias 핀닝</td><td><code>&#123;ENV&#125;-&#123;빌드번호&#125;</code> 자동</td><td><code>&#123;ENV&#125;-&#123;날짜&#125;-&#123;SEQ&#125;</code> 자동</td></tr><tr><td>SF definition 교체</td><td>없음 (수동)</td><td>자동</td></tr><tr><td>SF version·alias 핀닝</td><td>없음 (수동)</td><td>자동</td></tr><tr><td>dry-run 검증</td><td>없음</td><td>Stage 6 자동</td></tr><tr><td>프로젝트 추가</td><td>Jenkinsfile 복사·수정</td><td><code>projects/&#123;name&#125;.md</code> 추가</td></tr></tbody></table><p><img src="/images/aws-deploy-skill/metrics-comparison.png" alt="측정 비교 — Jenkins 평균 총 소요 vs aws-deploy Stage 1 분석"></p><hr><h2 id="Jenkins-대비-포기하는-것"><a href="#Jenkins-대비-포기하는-것" class="headerlink" title="Jenkins 대비 포기하는 것"></a>Jenkins 대비 포기하는 것</h2><p>이 스킬은 Jenkins를 완전히 대체하는 범용 CI가 아니다. 선택 전에 기능적 손실과 운영 리스크를 같이 봐야 한다.</p><p><strong>기능적 손실</strong></p><ul><li><strong>빌드 이력 UI 없음</strong> — Jenkins처럼 중앙 히스토리 화면이 기본 제공되지 않는다.</li><li><strong>병렬 배포 최적화 없음</strong> — 기본 설계는 순차 실행 기준이다.</li><li><strong>알림 연동은 별도 구성</strong> — Slack 등 배포 완료 알림은 직접 붙여야 한다.</li></ul><p><strong>운영 리스크</strong></p><ul><li><strong>Claude Code 의존</strong> — Claude Code CLI가 없는 환경에서는 실행할 수 없다.</li><li><strong>중앙 감사 추적 약화</strong> — 누가 언제 어떤 로컬 환경에서 배포했는지를 한 화면에서 추적하기 어렵다.</li><li><strong>로컬 자격증명 의존</strong> — AWS CLI 프로파일과 로컬 환경 상태가 배포 품질에 직접 영향을 준다.</li><li><strong>팀 표준 경로 분기 가능성</strong> — Jenkins와 병행 운영하면 “공식 배포 경로”가 둘이 될 수 있다.</li><li><strong>거버넌스 충돌 가능성</strong> — 승인 절차가 중요한 조직에서는 로컬 실행형 배포가 운영 통제 정책과 부딪힐 수 있다.</li></ul><p><strong>이 선택이 맞는 상황</strong>: 소규모 팀이 직접 배포를 관리하고, 로컬 맥락에서 빠르게 변경 감지와 배포를 이어가야 하는 경우.</p><p><strong>Jenkins가 더 나은 상황</strong>: 여러 팀원이 독립적으로 배포하고, 중앙 UI·감사 추적·알림 체계가 필수인 경우.</p><hr><h2 id="결론"><a href="#결론" class="headerlink" title="결론"></a>결론</h2><p>upstream 기준 변경 감지, Lambda alias 자동 핀닝, Step Functions definition 교체, SF version·alias 핀닝, Stage 6 dry-run 검증. aws-deploy 스킬은 Jenkins가 비워둔 일부 영역을 로컬 작업 맥락 안에서 메운다.</p><p>하지만 이 글의 결론은 “Jenkins보다 빨랐다”가 아니다.</p><blockquote><p>인프라만 알던 DevOps 담당자도, MCP처럼 외부로 내보낼 필요 없이, 자신의 작업 패턴을 Skills로 코드화할 수 있다. 안정화되면 팀과 공유해서 암묵지를 명시지로 바꾸는 발판이 된다.</p></blockquote><p>몇 번의 수동 AWS CLI 배포가 Claude Code 안에서 재현 가능해졌고, 그걸 Skill로 정리하는 순간 개인 루틴이 전달 가능한 프로세스로 바뀌었다. 이 글은 그 기록이다.</p>]]></content>
      
      
      <categories>
          
          <category> 인프라 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> AWS </tag>
            
            <tag> Lambda </tag>
            
            <tag> StepFunctions </tag>
            
            <tag> ClaudeCode </tag>
            
            <tag> DevOps </tag>
            
        </tags>
      
    </entry>
    
    
  
  
</search>
